<!DOCTYPE html>
<html>
<head>
  <title><%= full_title(yield(:title)) %></title>
  <meta charset="utf-8">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <!-- Replace 'data-turbolinks-track' with 'data-turbo-track' for Turbo Drive in Rails 7 -->
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  <!-- Replace 'javascript_pack_tag' with 'javascript_importmap_tags' if using Importmaps -->
  <%= javascript_importmap_tags %>
  <!-- Continue to include shim for legacy browser support -->
  <%= render 'layouts/shim' %>
</head>
<body>
  <!-- Render header from a partial -->
  <%= render 'layouts/header' %>
  <div class="container">
    <% flash.each do |message_type, message| %>
    <div class="alert alert-<%= message_type %>"><%= message %></div>
    <% end %>
    <!-- Render footer from a partial -->
    <%= yield %>
    <%= render 'layouts/footer' %>
    <%= debug(params) if Rails.env.development? %>
  </div>
</body>
</html>
